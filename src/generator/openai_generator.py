from src.generator.base_generator import (
    BaseGenerator
)

class OpenAIGenerator(
    BaseGenerator
):

    def run(
        self,
        prompt
    ):

        raise NotImplementedError(
            "connect later"
        )